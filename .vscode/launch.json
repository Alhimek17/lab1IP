{
    "configurations": [
        {
            "type": "java",
            "name": "Server",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "mainClass": "ru.ulstu.is.server.ServerApplication",
            "projectName": "lec1",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}
